<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite HTTP sin cifrar SOLO hacia el backend local de pruebas.
  Producción (Railway, HTTPS) sigue protegida. Quitar/ajustar para release.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.18.65</domain> <!-- PC en LAN (dispositivo físico) -->
        <domain includeSubdomains="false">10.0.2.2</domain>       <!-- host del emulador Android -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
